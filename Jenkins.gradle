pipeline{
    agent{label 'RRR'}
    stages{
        stage ('vcs'){
            steps{
                git url: "https://github.com/hemachaitanya/spring-petclinic.git",
                    branch:"main"
            }
        }
        stage ('build'){
            steps{
            sh "./gradlew build"
            } 
        }      
    }
}